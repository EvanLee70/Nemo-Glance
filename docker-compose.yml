services:
  glance:
    image: glanceapp/glance:latest
    container_name: nemo-glance
    restart: unless-stopped
    ports:
      - "${PORT}:8080"
    volumes:
      - ./glance.yml:/app/config/glance.yml:ro
